/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  8
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      fvSolution;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

simpleSolver SIMPLE;    /*1. SIMPLE; 2. SIMPLEC */
angle  59;

solvers
{
    p
    {
        solver          GAMG;
        smoother        GaussSeidel;
        tolerance       1e-6;
        relTol          0.01; 
        nPreSweeps			0;
        nPostSweeps			2;
        cacheAgglomeration	on;
        agglomerator		faceAreaPair;
        nCellsInCoarsestLevel	1000;
        mergeLevels			1;  
    }
    
    pFinal
    {
        $p;
        smoother        DICGaussSeidel;
        tolerance       1e-06;
        relTol          0;
    }

    "(U|k|B|nuTilda|s|omega|epsilon)"
    {
        solver          smoothSolver;
        smoother        symGaussSeidel;
        tolerance       1e-8;
        relTol          0;
    }
    Phi
    {
        solver          GAMG;
        smoother        DIC;

        tolerance       1e-07;
        relTol          0.01;
    }
    
}

SIMPLE
{
    residualControl
    {
        p               1e-3;
        U               1e-4;
        "(k|omega|epsilon)" 1e-4;
    }
   
   #ifeq $simpleSolver SIMPLE
    consistent			no;
   #else
    consistent			yes;
   #endif   
    
  #if $angle >85
		nNonOrthogonalCorrectors 5;
	#endif
	
	#if 70 <= $angle <85
		nNonOrthogonalCorrectors 3;
	#endif
		
	#if 60 <= $angle <70 
    nNonOrthogonalCorrectors 2;
  #endif    
   
  #if $angle < 60 
  	nNonOrthogonalCorrectors 1;
  #endif    
    
    pRefCell        0;
    pRefValue       0;
}

PISO
{
    nCorrectors     2;
    
  #if $angle >85
		nNonOrthogonalCorrectors 5;
	#endif
	
	#if 70 <= $angle <85
		nNonOrthogonalCorrectors 3;
	#endif
		
	#if 60 <= $angle <70 
    nNonOrthogonalCorrectors 2;
  #endif    
   
  #if $angle < 60 
  	nNonOrthogonalCorrectors 1;
  #endif    
  
    pRefPoint (0 0 0);
    pRefValue 0;
}



potentialFlow
{
    nNonOrthogonalCorrectors 10;
}

relaxationFactors
{
   #ifeq $simpleSolver SIMPLE
    fields
    {
        p               0.3;
    }
    equations
    {
        U               0.7;
        "(k|omega|epsilon).*" 0.7 ;
    }  
    
   #else
    fields
    {
        p               1;
    }
    equations
    {
        U               0.9;
        "(k|omega|epsilon).*" 0.9;
    }
   #endif   
    
    
    
}

// ************************************************************************* //
